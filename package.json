{
    "private": true,
    "type": "module",
    "scripts": {
        "dev": "concurrently \"npm run vite\" \"php artisan serve\"",
        "vite": "vite --host",
        "build": "vite build"
    },
    "devDependencies": {
        "axios": "^1.6.4",
        "concurrently": "^9.1.2",
        "laravel-vite-plugin": "^1.0.0",
        "vite": "^5.0.0"
    }
}
